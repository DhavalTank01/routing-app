import React from "react";

const Footer = () => {
  return (
    <div className="text-uppercase text-center">
      {new Date().getFullYear().toString()} @ all rights reserverd
    </div>
  );
};

export default Footer;
